# TC-RADAR API dependencies
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
starlette>=0.36

# Scientific / data
numpy>=1.24,<2.0
xarray>=2024.1
scipy>=1.12
matplotlib>=3.8
Pillow>=10.0

# NetCDF / Zarr / Cloud Storage
netCDF4>=1.6
h5netcdf>=1.3
h5py>=3.10
# fsspec family PINNED (2026-06-30): a gcsfs mid-version regression issued one
# ListObjects per zarr chunk read (a `_get_directory_info` directory probe on
# every chunk key) → ~66k Class-A ops/day (~$10/mo) on tc-atlas-zarr. These exact
# versions were verified to do ZERO per-chunk lists against the live store. Keep
# fsspec/gcsfs/s3fs pinned in lockstep; re-verify the ListObjects metric before
# bumping. See project_cost_status_2026_06 memory.
fsspec==2026.2.0
zarr>=2.16,<3.0
s3fs==2026.2.0
gcsfs==2026.2.0

# GRIB2 parsing (GFS shear endpoint pulls subsetted GRIB2 from NOMADS
# cgi-bin filter and reads u/v at 200/850 hPa with cfgrib).
cfgrib>=0.9.10
eccodes>=2.34

# Microwave API (TC-PRIMED S3 access)
boto3>=1.34

# Microwave NRT ingest (mw_ingest.py): shapely for footprint polygons,
# scikit-image for valid-pixel mask → contour extraction (skimage.measure).
shapely>=2.0
scikit-image>=0.22

# NEXRAD Level II radar (WSR-88D ground radar overlay)
arm-pyart>=1.18

# Realtime TDR module (lazy-loaded)
pyproj>=3.6
requests>=2.31

# Google Cloud Storage (IR frame cache)
google-cloud-storage>=2.14

# RT Seasonal Panel B Daily view — /seasonal/daily endpoint reads
# indices_daily_full.parquet from GCS via pandas.read_parquet, which
# requires a parquet engine (pyarrow is what the matching daily Cloud
# Run Job already uses).
pandas>=2.0
pyarrow>=15.0

# ASGI
gunicorn>=21.2
